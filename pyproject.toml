[project]
name = "bellwether"
version = "0.1.0"
description = "Subscription churn prediction & autonomous intervention pipeline"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
